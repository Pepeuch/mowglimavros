Configuration defaults for `mowgli_ntrip_client` are provided through launch arguments
and environment variables. This directory is kept so the package installs a stable
`share/<pkg>/config` path for sidecar deployments.
